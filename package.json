{
  "name": "plugowtech",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx postcss public/page/styles.css -o build/styles.css",
    "build:css": "npx tailwindcss -i ./public/input.css -o ./public/output.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.16"
  }
}
